@model List<Author>

@{ int count = 0;}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h1 class="card-title .h1">Author</h1>

            <a asp-action="create" asp-controller="author" class="btn btn-success">
                Create Author
            </a>

            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                No
                            </th>
                            <th>
                                Fullname
                            </th>
                            <th>
                                Blog count
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>
                                <td>
                                    @item.Name @item.Surname
                                </td>
                                <td>
                                    @item.Blogs.Count
                                </td>
                                <td>
                                    <div class="btn-group">
                                        <a asp-action="Delete" asp-controller="Author" asp-route-id="@item.Id" class="btn-sm btn btn-danger">
                                            <i class="mdi mdi-delete-forever btn-icon-primary">
                                            </i>
                                        </a>
                                        <a asp-action="Update" asp-controller="Author" asp-route-id="@item.Id" class="btn-sm btn btn-warning">
                                            <i class="mdi mdi-lead-pencil btn-icon-primary">
                                            </i>
                                        </a>
                                        <a asp-action="Detail" asp-controller="Author" asp-route-id="@item.Id" class="btn-sm btn btn-primary">
                                            <i class="mdi mdi-open-in-new btn-icon-primary">
                                            </i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>



